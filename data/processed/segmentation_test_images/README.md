# Segmentation UI Test Images

Use these copied NDVI sample images to test the segmentation PoC UI.

Run the UI from the project root:

```bash
source .venv/bin/activate
python -m crop_fusion_ai.ui.tkinter_app
```

Then select any `test_*.png` image from this folder and click Run Prediction.
