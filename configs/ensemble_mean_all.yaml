model_name: ensemble_mean
target_mode: seasonal_residual
loss_mode: raw_mse
feature_group: all
seq_len: 6
known_months: [0, 1, 3, 6, 9]
blank_fill_year: 2021
run_dir: outputs/experiments/ia30_seq6_seasonal_residual_blankfill
output_dir: outputs/experiments/handover_ensemble_mean_all
notes:
  - "Reference-only config for report and ensemble comparison usage."
  - "Use the strict blank-fill artifact summaries for deployable ensemble comparisons."
components:
  - seasonal_last_year
  - lstm_seasonal_residual
  - tiny_mamba_ssm_seasonal_residual
  - sarima
